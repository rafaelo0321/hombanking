/* let clientsData = []
axios.get('/api/clients')
    .then(res => {
        let dataResponse = res.data
        clientsData = dataResponse
    })
function dateFormat(formart) {
    formart = new Date(formart).toLocaleDateString();
    return formart;
}
//Funsiones que toman una fecha y la convierten a mes y año
function dataFormatCardsMonth(dataCards) {
    
    return dataCards.getMonth()
}
function dataFormatCardsYear(dataCards) {
    
    return dataCards.getFullYear()
} */
/* axios.post('/api/login',"email=jack@goto.com&password=lolita",{headers:{'content-type':'application/x-www-form-urlencoded'}}).then(response => console.log('signed in!!!')) */